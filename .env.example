# Browser-visible API root, including /api. Use /api behind a same-origin reverse proxy.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api
NEXT_PUBLIC_GUTEN_SITES_URL=http://localhost:3000

# Show Sign out only when deployed behind the authentication proxy. Build-time setting.
NEXT_PUBLIC_AUTH_ENABLED=false
